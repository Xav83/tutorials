---
steps:
  - checkout: self
    displayName: Checks out the repository
  - script: |
      pip install black
    displayName: Installs the latest version of yamllint
  - script: |
      black  --check .
    displayName: Runs black on all the python files of the repository to check if they are formatted properly
